
# Prototype Pollution Packages

This directory contains the curated datasets from Ferreira et al.'s works. Each sub-directory is a NPM package. Note: Install the dependencies before using the package.